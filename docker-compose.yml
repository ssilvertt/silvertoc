version: "3.9"

services:
  telegram-bot:
    build: .
    container_name: telegram-bot
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${BRIDGE_PORT:-3000}:3000"
    volumes:
      - ./data:/app/data
